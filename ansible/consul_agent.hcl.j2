datacenter = "dc1"
data_dir   = "/opt/consul"

bind_addr = "{{ ansible_default_ipv4.address }}"
advertise_addr = "{{ ansible_default_ipv4.address }}"

start_join = ["{{ hostvars[groups['consul_servers'][0]].ansible_default_ipv4.address }}"]

client_addr = "0.0.0.0"
